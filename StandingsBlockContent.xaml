<UserControl x:Class="NBAInformer.StandingsBlockContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NBAInformer"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
    <Grid Style="{StaticResource StandinsBlockStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="60"></ColumnDefinition>
            <ColumnDefinition Width="40*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Image Grid.Column="0" x:Name="TeamLogoStandings"/>
        <TextBlock Grid.Column="1" x:Name="TeamNameStandings" Style="{StaticResource TeamInfoStandingsStyle}"></TextBlock>
        <TextBlock Grid.Column="2" x:Name="TeamGameStatStandings" Style="{StaticResource TeamInfoStandingsStyle}"></TextBlock>
        <TextBlock Grid.Column="3" x:Name="TeamGameKStandings" Style="{StaticResource TeamInfoStandingsStyle}"></TextBlock>
    </Grid>
</UserControl>
